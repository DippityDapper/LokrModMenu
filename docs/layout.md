# LokrModMenu — Layout

```
LokrModMenu/
├── LokrModMenuPlugin.cs    ← entry, config, hotkeys
├── ModMenuAPI.cs           ← public registration API
├── ModMenuOverlay.cs       ← persistent menu UI scene
└── ModMenuInputHandler.cs  ← escape / back handling
```
